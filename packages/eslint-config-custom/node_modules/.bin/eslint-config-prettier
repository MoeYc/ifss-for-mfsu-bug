#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/aluo/Desktop/githubs/ifss/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/Users/aluo/Desktop/githubs/ifss/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../node_modules/.pnpm/eslint-config-prettier@8.7.0_eslint@8.0.0-rc.0/node_modules/eslint-config-prettier/bin/cli.js" "$@"
else
  exec node  "$basedir/../../../../node_modules/.pnpm/eslint-config-prettier@8.7.0_eslint@8.0.0-rc.0/node_modules/eslint-config-prettier/bin/cli.js" "$@"
fi
